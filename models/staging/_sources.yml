version: 2

sources:
  - name: sakila_raw
    database: majeds-gcp
    schema: sakila_raw
    tables:
      - name: v_stg_actor
        description: "Deduplicated staging view over raw GCS CDC actor events."